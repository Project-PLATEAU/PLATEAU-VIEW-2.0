// Code generated by github.com/vektah/dataloaden, DO NOT EDIT.

package gqldataloader

import (
	"sync"
	"time"

	"github.com/reearth/reearth-cms/server/internal/adapter/gql/gqlmodel"
)

// AssetItemLoaderConfig captures the config to create a new AssetItemLoader
type AssetItemLoaderConfig struct {
	// Fetch is a method that provides the data for the loader
	Fetch func(keys []gqlmodel.ID) ([][]*gqlmodel.AssetItem, []error)

	// Wait is how long wait before sending a batch
	Wait time.Duration

	// MaxBatch will limit the maximum number of keys to send in one batch, 0 = not limit
	MaxBatch int
}

// NewAssetItemLoader creates a new AssetItemLoader given a fetch, wait, and maxBatch
func NewAssetItemLoader(config AssetItemLoaderConfig) *AssetItemLoader {
	return &AssetItemLoader{
		fetch:    config.Fetch,
		wait:     config.Wait,
		maxBatch: config.MaxBatch,
	}
}

// AssetItemLoader batches and caches requests
type AssetItemLoader struct {
	// this method provides the data for the loader
	fetch func(keys []gqlmodel.ID) ([][]*gqlmodel.AssetItem, []error)

	// how long to done before sending a batch
	wait time.Duration

	// this will limit the maximum number of keys to send in one batch, 0 = no limit
	maxBatch int

	// INTERNAL

	// lazily created cache
	cache map[gqlmodel.ID][]*gqlmodel.AssetItem

	// the current batch. keys will continue to be collected until timeout is hit,
	// then everything will be sent to the fetch method and out to the listeners
	batch *assetItemLoaderBatch

	// mutex to prevent races
	mu sync.Mutex
}

type assetItemLoaderBatch struct {
	keys    []gqlmodel.ID
	data    [][]*gqlmodel.AssetItem
	error   []error
	closing bool
	done    chan struct{}
}

// Load a AssetItem by key, batching and caching will be applied automatically
func (l *AssetItemLoader) Load(key gqlmodel.ID) ([]*gqlmodel.AssetItem, error) {
	return l.LoadThunk(key)()
}

// LoadThunk returns a function that when called will block waiting for a AssetItem.
// This method should be used if you want one goroutine to make requests to many
// different data loaders without blocking until the thunk is called.
func (l *AssetItemLoader) LoadThunk(key gqlmodel.ID) func() ([]*gqlmodel.AssetItem, error) {
	l.mu.Lock()
	if it, ok := l.cache[key]; ok {
		l.mu.Unlock()
		return func() ([]*gqlmodel.AssetItem, error) {
			return it, nil
		}
	}
	if l.batch == nil {
		l.batch = &assetItemLoaderBatch{done: make(chan struct{})}
	}
	batch := l.batch
	pos := batch.keyIndex(l, key)
	l.mu.Unlock()

	return func() ([]*gqlmodel.AssetItem, error) {
		<-batch.done

		var data []*gqlmodel.AssetItem
		if pos < len(batch.data) {
			data = batch.data[pos]
		}

		var err error
		// its convenient to be able to return a single error for everything
		if len(batch.error) == 1 {
			err = batch.error[0]
		} else if batch.error != nil {
			err = batch.error[pos]
		}

		if err == nil {
			l.mu.Lock()
			l.unsafeSet(key, data)
			l.mu.Unlock()
		}

		return data, err
	}
}

// LoadAll fetches many keys at once. It will be broken into appropriate sized
// sub batches depending on how the loader is configured
func (l *AssetItemLoader) LoadAll(keys []gqlmodel.ID) ([][]*gqlmodel.AssetItem, []error) {
	results := make([]func() ([]*gqlmodel.AssetItem, error), len(keys))

	for i, key := range keys {
		results[i] = l.LoadThunk(key)
	}

	assetItems := make([][]*gqlmodel.AssetItem, len(keys))
	errors := make([]error, len(keys))
	for i, thunk := range results {
		assetItems[i], errors[i] = thunk()
	}
	return assetItems, errors
}

// LoadAllThunk returns a function that when called will block waiting for a AssetItems.
// This method should be used if you want one goroutine to make requests to many
// different data loaders without blocking until the thunk is called.
func (l *AssetItemLoader) LoadAllThunk(keys []gqlmodel.ID) func() ([][]*gqlmodel.AssetItem, []error) {
	results := make([]func() ([]*gqlmodel.AssetItem, error), len(keys))
	for i, key := range keys {
		results[i] = l.LoadThunk(key)
	}
	return func() ([][]*gqlmodel.AssetItem, []error) {
		assetItems := make([][]*gqlmodel.AssetItem, len(keys))
		errors := make([]error, len(keys))
		for i, thunk := range results {
			assetItems[i], errors[i] = thunk()
		}
		return assetItems, errors
	}
}

// Prime the cache with the provided key and value. If the key already exists, no change is made
// and false is returned.
// (To forcefully prime the cache, clear the key first with loader.clear(key).prime(key, value).)
func (l *AssetItemLoader) Prime(key gqlmodel.ID, value []*gqlmodel.AssetItem) bool {
	l.mu.Lock()
	var found bool
	if _, found = l.cache[key]; !found {
		// make a copy when writing to the cache, its easy to pass a pointer in from a loop var
		// and end up with the whole cache pointing to the same value.
		cpy := make([]*gqlmodel.AssetItem, len(value))
		copy(cpy, value)
		l.unsafeSet(key, cpy)
	}
	l.mu.Unlock()
	return !found
}

// Clear the value at key from the cache, if it exists
func (l *AssetItemLoader) Clear(key gqlmodel.ID) {
	l.mu.Lock()
	delete(l.cache, key)
	l.mu.Unlock()
}

func (l *AssetItemLoader) unsafeSet(key gqlmodel.ID, value []*gqlmodel.AssetItem) {
	if l.cache == nil {
		l.cache = map[gqlmodel.ID][]*gqlmodel.AssetItem{}
	}
	l.cache[key] = value
}

// keyIndex will return the location of the key in the batch, if its not found
// it will add the key to the batch
func (b *assetItemLoaderBatch) keyIndex(l *AssetItemLoader, key gqlmodel.ID) int {
	for i, existingKey := range b.keys {
		if key == existingKey {
			return i
		}
	}

	pos := len(b.keys)
	b.keys = append(b.keys, key)
	if pos == 0 {
		go b.startTimer(l)
	}

	if l.maxBatch != 0 && pos >= l.maxBatch-1 {
		if !b.closing {
			b.closing = true
			l.batch = nil
			go b.end(l)
		}
	}

	return pos
}

func (b *assetItemLoaderBatch) startTimer(l *AssetItemLoader) {
	time.Sleep(l.wait)
	l.mu.Lock()

	// we must have hit a batch limit and are already finalizing this batch
	if b.closing {
		l.mu.Unlock()
		return
	}

	l.batch = nil
	l.mu.Unlock()

	b.end(l)
}

func (b *assetItemLoaderBatch) end(l *AssetItemLoader) {
	b.data, b.error = l.fetch(b.keys)
	close(b.done)
}
